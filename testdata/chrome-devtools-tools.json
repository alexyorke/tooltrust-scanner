{
  "tools": [
    {
      "name": "click",
      "description": "Clicks on the provided element",
      "inputSchema": {
        "type": "object",
        "properties": {
          "uid": { "type": "string", "description": "The uid of an element on the page from the page content snapshot" },
          "dblClick": { "type": "boolean", "description": "Set to true for double clicks. Default is false." },
          "includeSnapshot": { "type": "boolean", "description": "Whether to include a snapshot in the response. Default is false." }
        },
        "required": ["uid"]
      }
    },
    {
      "name": "drag",
      "description": "Drag an element onto another element",
      "inputSchema": {
        "type": "object",
        "properties": {
          "from_uid": { "type": "string", "description": "The uid of the element to drag" },
          "to_uid": { "type": "string", "description": "The uid of the element to drop into" },
          "includeSnapshot": { "type": "boolean", "description": "Whether to include a snapshot in the response." }
        },
        "required": ["from_uid", "to_uid"]
      }
    },
    {
      "name": "fill",
      "description": "Type text into a input, text area or select an option from a <select> element.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "uid": { "type": "string", "description": "The uid of an element on the page" },
          "value": { "type": "string", "description": "The value to fill in" },
          "includeSnapshot": { "type": "boolean", "description": "Whether to include a snapshot in the response." }
        },
        "required": ["uid", "value"]
      }
    },
    {
      "name": "fill_form",
      "description": "Fill out multiple form elements at once",
      "inputSchema": {
        "type": "object",
        "properties": {
          "elements": { "type": "array", "description": "Elements from snapshot to fill out." },
          "includeSnapshot": { "type": "boolean", "description": "Whether to include a snapshot in the response." }
        },
        "required": ["elements"]
      }
    },
    {
      "name": "handle_dialog",
      "description": "If a browser dialog was opened, use this command to handle it",
      "inputSchema": {
        "type": "object",
        "properties": {
          "action": { "type": "string", "enum": ["accept", "dismiss"], "description": "Whether to dismiss or accept the dialog" },
          "promptText": { "type": "string", "description": "Optional prompt text to enter into the dialog." }
        },
        "required": ["action"]
      }
    },
    {
      "name": "hover",
      "description": "Hover over the provided element",
      "inputSchema": {
        "type": "object",
        "properties": {
          "uid": { "type": "string", "description": "The uid of an element on the page" },
          "includeSnapshot": { "type": "boolean", "description": "Whether to include a snapshot in the response." }
        },
        "required": ["uid"]
      }
    },
    {
      "name": "press_key",
      "description": "Press a key or key combination. Use this when other input methods like fill() cannot be used (e.g., keyboard shortcuts, navigation keys, or special key combinations).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "key": { "type": "string", "description": "A key or a combination (e.g., Enter, Control+A). Modifiers: Control, Shift, Alt, Meta" },
          "includeSnapshot": { "type": "boolean", "description": "Whether to include a snapshot in the response." }
        },
        "required": ["key"]
      }
    },
    {
      "name": "type_text",
      "description": "Type text using keyboard into a previously focused input",
      "inputSchema": {
        "type": "object",
        "properties": {
          "text": { "type": "string", "description": "The text to type" },
          "submitKey": { "type": "string", "description": "Optional key to press after typing. E.g., Enter, Tab, Escape" }
        },
        "required": ["text"]
      }
    },
    {
      "name": "upload_file",
      "description": "Upload a file through a provided element.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filePath": { "type": "string", "description": "The local path of the file to upload" },
          "uid": { "type": "string", "description": "The uid of the file input element" },
          "includeSnapshot": { "type": "boolean", "description": "Whether to include a snapshot in the response." }
        },
        "required": ["filePath", "uid"]
      }
    },
    {
      "name": "close_page",
      "description": "Closes the page by its index. The last open page cannot be closed.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "pageId": { "type": "number", "description": "The ID of the page to close. Call list_pages to list pages." }
        },
        "required": ["pageId"]
      }
    },
    {
      "name": "list_pages",
      "description": "Get a list of pages open in the browser.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "navigate_page",
      "description": "Navigates the currently selected page to a URL.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "url": { "type": "string", "description": "Target URL (only when type=url)" },
          "type": { "type": "string", "enum": ["url", "back", "forward", "reload"], "description": "Navigate by URL, back, forward, or reload." },
          "handleBeforeUnload": { "type": "string", "enum": ["accept", "decline"], "description": "Whether to auto accept beforeunload dialogs." },
          "ignoreCache": { "type": "boolean", "description": "Whether to ignore cache on reload." },
          "initScript": { "type": "string", "description": "A JavaScript script to be executed on each new document before any other scripts for the next navigation." },
          "timeout": { "type": "integer", "description": "Maximum wait time in milliseconds." }
        }
      }
    },
    {
      "name": "new_page",
      "description": "Creates a new page",
      "inputSchema": {
        "type": "object",
        "properties": {
          "url": { "type": "string", "description": "URL to load in a new page." },
          "background": { "type": "boolean", "description": "Whether to open in background. Default is false." },
          "isolatedContext": { "type": "string", "description": "Create page in isolated browser context with given name." },
          "timeout": { "type": "integer", "description": "Maximum wait time in milliseconds." }
        },
        "required": ["url"]
      }
    },
    {
      "name": "select_page",
      "description": "Select a page as a context for future tool calls.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "pageId": { "type": "number", "description": "The ID of the page to select. Call list_pages to get available pages." },
          "bringToFront": { "type": "boolean", "description": "Whether to focus the page and bring it to the top." }
        },
        "required": ["pageId"]
      }
    },
    {
      "name": "wait_for",
      "description": "Wait for the specified text to appear on the selected page.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "text": { "type": "array", "description": "Non-empty list of texts. Resolves when any value appears on the page." },
          "timeout": { "type": "integer", "description": "Maximum wait time in milliseconds." }
        },
        "required": ["text"]
      }
    },
    {
      "name": "emulate",
      "description": "Emulates various features on the selected page.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "colorScheme": { "type": "string", "enum": ["dark", "light", "auto"], "description": "Emulate dark or light mode." },
          "cpuThrottlingRate": { "type": "number", "description": "CPU slowdown factor. Omit or 1 to disable." },
          "geolocation": { "type": "string", "description": "Geolocation (latitudexlongitude) to emulate." },
          "networkConditions": { "type": "string", "enum": ["Offline", "Slow 3G", "Fast 3G", "Slow 4G", "Fast 4G"], "description": "Throttle network." },
          "userAgent": { "type": "string", "description": "User agent to emulate." },
          "viewport": { "type": "string", "description": "Emulate device viewports widthxheightxdevicePixelRatio[,mobile][,touch][,landscape]." }
        }
      }
    },
    {
      "name": "resize_page",
      "description": "Resizes the selected page's window so that the page has specified dimension",
      "inputSchema": {
        "type": "object",
        "properties": {
          "height": { "type": "number", "description": "Page height" },
          "width": { "type": "number", "description": "Page width" }
        },
        "required": ["height", "width"]
      }
    },
    {
      "name": "performance_analyze_insight",
      "description": "Provides more detailed information on a specific Performance Insight of an insight set that was highlighted in the results of a trace recording.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "insightName": { "type": "string", "description": "The name of the Insight (e.g. DocumentLatency or LCPBreakdown)" },
          "insightSetId": { "type": "string", "description": "The id for the specific insight set." }
        },
        "required": ["insightName", "insightSetId"]
      }
    },
    {
      "name": "performance_start_trace",
      "description": "Starts a performance trace recording on the selected page. Reports Core Web Vital (CWV) scores.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "autoStop": { "type": "boolean", "description": "Auto stop the trace recording." },
          "filePath": { "type": "string", "description": "Path to save raw trace data (e.g. trace.json.gz)." },
          "reload": { "type": "boolean", "description": "Reload page once tracing has started." }
        }
      }
    },
    {
      "name": "performance_stop_trace",
      "description": "Stops the active performance trace recording on the selected page.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filePath": { "type": "string", "description": "Path to save raw trace data." }
        }
      }
    },
    {
      "name": "take_memory_snapshot",
      "description": "Capture a memory heapsnapshot of the currently selected page to memory leak debugging",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filePath": { "type": "string", "description": "Path to .heapsnapshot file to save the heapsnapshot to." }
        },
        "required": ["filePath"]
      }
    },
    {
      "name": "get_network_request",
      "description": "Gets a network request by an optional reqid, if omitted returns the currently selected request in the DevTools Network panel.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "reqid": { "type": "number", "description": "The reqid of the network request." },
          "requestFilePath": { "type": "string", "description": "Path to save the request body to." },
          "responseFilePath": { "type": "string", "description": "Path to save the response body to." }
        }
      }
    },
    {
      "name": "list_network_requests",
      "description": "List all requests for the currently selected page since the last navigation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "includePreservedRequests": { "type": "boolean", "description": "Return preserved requests over last 3 navigations." },
          "pageIdx": { "type": "integer", "description": "Page number to return (0-based)." },
          "pageSize": { "type": "integer", "description": "Maximum number of requests to return." },
          "resourceTypes": { "type": "array", "description": "Filter by resource types." }
        }
      }
    },
    {
      "name": "evaluate_script",
      "description": "Evaluate a JavaScript function inside the currently selected page. Returns the response as JSON, so returned values have to be JSON-serializable.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "function": {
            "type": "string",
            "description": "A JavaScript function declaration to be executed by the tool in the currently selected page. Example: () => { return document.title } or async () => { return await fetch('example.com') }. Example with arguments: (el) => { return el.innerText; }"
          },
          "args": { "type": "array", "description": "An optional list of arguments to pass to the function." }
        },
        "required": ["function"]
      }
    },
    {
      "name": "get_console_message",
      "description": "Gets a console message by its ID. You can get all messages by calling list_console_messages.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "msgid": { "type": "number", "description": "The msgid of a console message on the page." }
        },
        "required": ["msgid"]
      }
    },
    {
      "name": "lighthouse_audit",
      "description": "Get Lighthouse score and reports for accessibility, SEO and best practices.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "device": { "type": "string", "enum": ["desktop", "mobile"], "description": "Device to emulate." },
          "mode": { "type": "string", "enum": ["navigation", "snapshot"], "description": "navigation reloads & audits. snapshot analyzes current state." },
          "outputDirPath": { "type": "string", "description": "Directory for reports." }
        }
      }
    },
    {
      "name": "list_console_messages",
      "description": "List all console messages for the currently selected page since the last navigation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "includePreservedMessages": { "type": "boolean", "description": "Return preserved messages over last 3 navigations." },
          "pageIdx": { "type": "integer", "description": "Page number (0-based)." },
          "pageSize": { "type": "integer", "description": "Maximum number of messages to return." },
          "types": { "type": "array", "description": "Filter by message types." }
        }
      }
    },
    {
      "name": "take_screenshot",
      "description": "Take a screenshot of the page or element.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filePath": { "type": "string", "description": "Path to save the screenshot." },
          "format": { "type": "string", "enum": ["png", "jpeg", "webp"], "description": "Format. Default is png" },
          "fullPage": { "type": "boolean", "description": "Screenshot full page. Incompatible with uid." },
          "quality": { "type": "number", "description": "JPEG/WebP quality 0-100." },
          "uid": { "type": "string", "description": "Uid of element. If omitted takes page screenshot." }
        }
      }
    },
    {
      "name": "take_snapshot",
      "description": "Take a text snapshot of the currently selected page based on the a11y tree. The snapshot lists page elements along with a unique identifier (uid). Always use the latest snapshot.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filePath": { "type": "string", "description": "Path to save the snapshot." },
          "verbose": { "type": "boolean", "description": "Include full a11y tree. Default is false." }
        }
      }
    }
  ]
}
